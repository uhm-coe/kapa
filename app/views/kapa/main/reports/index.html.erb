<% content_for(:box1) do %>

  <h3>Reports</h3>

  <table class="table table-striped">
    <thead>
    <tr>
      <th>ID</th>
      <th>Type</th>
      <th>Name</th>
      <th>Datasource</th>
      <th>Loaded at</th>
      <th>Record Count</th>
      <th></th>
    </tr>
    </thead>
    <tbody>
    <% @datasets.each do |dataset| %>
      <tr>
        <td><%= link_to dataset.id, kapa_admin_dataset_path(dataset) %></td>
        <td><%= dataset.type %></td>
        <td><%= dataset.name %></td>
        <td><%= dataset.datasource %></td>
        <td><%= dataset.loaded_at %></td>
        <td><%= dataset.record_count %></td>
        <td class="right">
          <%= link_to "Show", kapa_main_report_path(dataset), :class => 'btn btn-default btn-xs' %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>

<% end %>
